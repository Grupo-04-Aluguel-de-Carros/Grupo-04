import { db } from "../../../config/db.js"

export const createFeatureRepo = async (massageSystem, shielding, sunRoof, gearLever, selfDriving, zeroToHundred, displacement, power, velocity) => {
    try {
        const feature = db.
    } catch (error) {
        
    }


}